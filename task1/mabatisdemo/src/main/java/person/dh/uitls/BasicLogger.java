package person.dh.uitls;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

//方法二：
public class BasicLogger {
	public static Logger logger = LogManager.getLogger(BasicLogger.class);
}
